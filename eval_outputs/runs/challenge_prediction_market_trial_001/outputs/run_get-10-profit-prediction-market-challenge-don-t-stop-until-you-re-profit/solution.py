from __future__ import annotations

from orderbook_pm_challenge.strategy import BaseStrategy
from orderbook_pm_challenge.types import CancelAll, PlaceOrder, Side, StepState


class Strategy(BaseStrategy):
    """Adaptive passive prediction-market market maker.

    Generated by research-harness from the best optimize-query variant.
    This file targets the public upstream challenge API:
    https://github.com/danrobinson/prediction-market-challenge

    Source variant:
    """  # noqa: D205

    quote_size = 5.0
    base_spread_ticks = 4
    inventory_limit = 90.0

    def __init__(self) -> None:
        self.estimated_mid_ticks = 50.0
        self.last_buy_fill = 0.0
        self.last_sell_fill = 0.0

    def on_step(self, state: StepState):
        competitor_bid = state.competitor_best_bid_ticks or 48
        competitor_ask = state.competitor_best_ask_ticks or 52
        competitor_mid = (competitor_bid + competitor_ask) / 2.0

        buy_flow = state.buy_filled_quantity
        sell_flow = state.sell_filled_quantity
        net_flow = buy_flow - sell_flow
        self.last_buy_fill = state.buy_filled_quantity
        self.last_sell_fill = state.sell_filled_quantity

        # Re-anchor gradually to the competitor midpoint, then use recent fills
        # as a weak retail-flow signal. Sudden midpoint changes are treated as
        # stale-quote risk, so we cancel before placing fresh orders.
        midpoint_jump = abs(competitor_mid - self.estimated_mid_ticks)
        self.estimated_mid_ticks = (self.estimated_mid_ticks * 0.82) + (competitor_mid * 0.18) + (net_flow * 0.08)

        inventory = state.yes_inventory - state.no_inventory
        inventory_skew = max(-4.0, min(4.0, inventory / 25.0))
        spread = self.base_spread_ticks + (2 if midpoint_jump >= 3 else 0)
        bid_ticks = int(max(1, min(98, round(self.estimated_mid_ticks - spread - inventory_skew))))
        ask_ticks = int(max(bid_ticks + 1, min(99, round(self.estimated_mid_ticks + spread - inventory_skew))))

        actions = [CancelAll()]
        ask_cost = max(0.01, ask_ticks / 100.0)
        bid_cost = max(0.01, bid_ticks / 100.0)
        size = max(1.0, min(self.quote_size, state.free_cash / ask_cost))

        if state.yes_inventory < self.inventory_limit and state.free_cash > bid_cost * size:
            actions.append(PlaceOrder(side=Side.BUY, price_ticks=bid_ticks, quantity=size))
        if state.no_inventory < self.inventory_limit:
            actions.append(PlaceOrder(side=Side.SELL, price_ticks=ask_ticks, quantity=size))
        return actions


SOURCE_VARIANT = """Prediction-market strategy sketch: adaptive fair value estimate from fills, order flow, and competitor midpoint; CancelAll or widen after stale adverse arbitrageur fills and jump volatility; passive limit orders only; size=5 spread=4 inventory limit=90 with inventory skew. Optimization variant informed by query finding: Get to $10 profit in the prediction market challenge, don't stop until you're profitable. Introduce entropy from AMM, prediction-market, and options literature before tweaking hyperparameters. foundational literature mechanisms refined pass 1"""
